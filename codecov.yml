coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%

flags:
  unit:
    paths:
      - src
    carryforward: false
  integration:
    paths:
      - src
    carryforward: false

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false

ignore:
  - "examples"
  - "docs"
  - "**/tests/**"

codecov:
  notify:
    wait_for_ci: false
  require_ci_to_pass: false